---
title: Accordion
description: Build vertically collapsing accordions with an integrated JS module.
layout: '@layouts/Component.astro'
variant: component
api:
  accordion:
    import: "import { Accordion } from 'astro-bootstrap'"
    props:
    - name: data
      type: "{ header: string; body: string; show?: boolean;}[]"
      default: ''
      description: Data to display in accordion, including the `show` property.
      required: true
    - name: flush
      type: boolean
      default: "false"
      description: Adds the 'accordion-flush' class to the accordion.
    - name: alwaysOpen
      type: boolean
      default: "false"
      description: Prevents Accordion Items from closing when another one is opened
    - name: id
      type: string
      default: 'null'
      description: Set the `id` on the `accordion` div.
    - name: class
      type: string
      default: 'null'
      description: Appends a class to the `accordion` div.
    - name: itemClass
      type: string
      default: 'null'
      description: Appends a class to the `accordion-item` div.
    - name: headerClass
      type: string
      default: 'null'
      description: Appends a class to the `accordion-header` div.
    - name: bodyClass
      type: string
      default: 'null'
      description: Appends a class to the `accordion-body` div.
---
import Example from '@components/Example.astro';
import Api from '@components/Api.astro';
import Standard from '@examples/Accordion/Standard.astro';
import rawStandard from '@examples/Accordion/Standard.astro?raw';
import Collapsed from '@examples/Accordion/Collapsed.astro';
import rawCollapsed from '@examples/Accordion/Collapsed.astro?raw';
import Flush from '@examples/Accordion/Flush.astro';
import rawFlush from '@examples/Accordion/Flush.astro?raw';
import AlwaysOpen from '@examples/Accordion/AlwaysOpen.astro';
import rawAlwaysOpen from '@examples/Accordion/AlwaysOpen.astro?raw';
import IdAndClasses from '@examples/Accordion/IdAndClasses.astro';
import rawIdAndClasses from '@examples/Accordion/IdAndClasses.astro?raw';
import NoComponents from '@examples/Accordion/NoComponents.astro';
import rawNoComponents from '@examples/Accordion/NoComponents.astro?raw';

## Examples

The following examples show how to implement the accordion with minimal code.

### Basic Example

<Example>
  <Standard slot="component" />
  <Fragment slot="code" set:html={rawStandard} />
</Example>

### Fully Collapsed State

If you want your `Accordion` to start in a fully-collapsed state, then simply don't add a `show: true` prop to any of the data elements.

<Example>
  <Collapsed slot="component" />
  <Fragment slot="code" set:html={rawCollapsed} />
</Example>

### Flush

Add `flush` to remove the default background-color, some borders, and some rounded corners to render accordions edge-to-edge with their parent container.

<Example>
  <Flush slot="component" />
  <Fragment slot="code" set:html={rawFlush} />
</Example>

### Always open

You can make accordion items stay open when another item is opened by using the `alwaysOpen` prop. If you're looking to
control the component, you must use an array of strings for `activeKey` or `defaultActiveKey`.

<Example>
  <AlwaysOpen slot="component" />
  <Fragment slot="code" set:html={rawAlwaysOpen} />
</Example>

### Set ID and classes

The following properties are available:

- `id` to add an id to the parent `accordion` div.
- `class` to append a class to the parent `accordion` div.
- `itemClass` to append a class to the `accordion-item` div.
- `headerClass` to append a class to the `accordion-header` div.
- `bodyClass` to append a class to the `accordion-body` div.

<Example>
  <IdAndClasses slot="component" />
  <Fragment slot="code" set:html={rawIdAndClasses} />
</Example>

### No Astro components

<Example>
  <NoComponents slot="component" />
  <Fragment slot="code" set:html={rawNoComponents} />
</Example>


## API

### Accordion 

<Api component={frontmatter.api.accordion} />
