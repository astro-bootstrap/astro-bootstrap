---
title: Button group
description: Group a series of buttons together on a single line or stack them in a vertical column.
docs: https://getbootstrap.com/docs/5.3/components/button-group/
layout: '@layouts/Component.astro'
variant: template
---
import Standard from '@examples/ButtonGroup/Standard.astro';
import rawStandard from '@examples/ButtonGroup/Standard.astro?raw';
import Example from '@components/Example.astro'

The button group HTML markup can be simplified and made more dry by adding the button text to an array, and iterating over it e.g.


<Example>
  <Standard slot="component"/>
  <Fragment slot="code" set:html={rawStandard} />
</Example>
