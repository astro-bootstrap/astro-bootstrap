---
title: Pagination
description: Documentation and examples for showing pagination to indicate a series of related content exists across multiple pages.
layout: '@layouts/Component.astro'
variant: component
api:
  pagination:
    import: "import { Pagination } from 'astro-bootstrap'"
    props:
      - name: page
        type: Astro page property
        default: ''
        description: Gives `Pagination` access to the page
        required: true
      - name: aria-label
        type: Astro page property
        default: Page pagination control
        description: Change the `aria-label`
      - name: class
        type: string
        default:
        description: Appends a class to the `pagination` `ul` tag.
      - name: itemClass
        type: string
        default:
        description: Appends a class to the `page-item` `li` tag.
      - name: linkClass
        type: string
        default:
        description: Appends a class to the `page-link` `anchor` tag.
      - name: delta
        type: number
        default: 2
        description: Number of pages to display either side of the current page in pagination widget.

---
import Example from '@components/Example.astro'
import Api from '@components/Api.astro';
import Basic from '@examples/components/Pagination/basic.html'
import rawBasic from './pagination-test/[...page].astro?raw'

## Examples

### Basic

The pagination component generates the pagination controls for the page.

You must set up the pagination through Astro's paginate function (see below). This component then accesses the `page` property to generate the pagination buttons.
{/* uses old method as pulls in non example code */}s
<Example>
<div class="d-flex justify-content-center" slot="component">
<Basic />
</div>
<Fragment slot="code" set:html={rawBasic}/>
</Example>

The rendered code is a static example, <a href="pagination-test"> click here to see a live demo.</a>

The Pagination component returns the default bootstrap pagination component, with the current page as an active link, and prev/next disabled if not available e.g.

You can optionally set the `aria-label` property with the `ariaLabel` prop e.g.

```
<Pagination page={page} aria-label="Pagination">
```

### Manual

{/*  
<Example component="Pagination" variant="Manual" /> */}

## API

### Pagination

<Api component={frontmatter.api.pagination} />
