---
title: Breadcrumb
description: Indicate the current page’s location within a navigational hierarchy that automatically adds separators via CSS.
layout: '@layouts/Component.astro'
variant: component
docs: https://getbootstrap.com/docs/5.3/components/breadcrumb/
api:
  breadcrumb:
    import: "import { Breadcrumb } from 'astro-bootstrap'"
    props:
    - name: aria-label
      type: string
      default: Breadcrumb
      description: Set the `aria-label` property value on the "breadcrumb" div.
    - name: id
      type: string
      default: 'null'
      description: Set the `id` on the "breadcrumb" div.
    - name: class
      type: string
      default: 'null'
      description: Set the `class` on the "breadcrumb" div.
    - name: itemClass
      type: string
      default: 'null'
      description: Set the `class` on the "breadcrumb-item" div.
---
import Example from '@components/Example.astro';
import Api from '@components/Api.astro';
import rawStandard from '@examples/Breadcrumb/Standard.astro?raw';
import Standard from '@examples/Breadcrumb/Standard.astro';
import rawProps from '@examples/Breadcrumb/Props.astro?raw';
import Props from '@examples/Breadcrumb/Props.astro';

## Examples

### Basic

<Example>
  <Standard slot="component"/>
  <Fragment slot="code" set:html={rawStandard} />
</Example>

### Set optional properties

<Example>
  <Props slot="component"/>
  <Fragment slot="code" set:html={rawProps} />
</Example>

## API

### Breadcrumb

<Api component={frontmatter.api.breadcrumb} />
