---
title: Tabs
variant: component
test: '@layouts/Component.astro'
docs: components/navs-tabs
api:
  tabs:
    import: "import { Tabs } from 'astro-bootstrap'"
    props:
      - name: tabs
        type: '{ title: string; body: string; active: boolean}[]'
        default: ''
        description: Data for populating tabs.
        required: true
      - name: id
        type: string
        default: ''
        description: Append `id` to `tab` div.
      - name: fade
        type: boolean
        default: true
        description: Enable fade transition between tabs.
      - name: listClass
        type: string
        default: ''
        description: Append `class` to `nav-tabs` div. (`Tabs.List` component)
      - name: itemClass
        type: string
        default: ''
        description: Append `class` to `nav-item` div.
      - name: contentClass
        type: string
        default: ''
        description: Append `class` to `nav-content` div.
      - name: paneClass
        type: string
        default: ''
        description: Append `class` to `nav-pane` div.
---

import Example from '@components/Example.astro';
import Api from '@components/Api.astro';

The `Tabs` component makes light work of creating a tabbed interface with the Bootstrap JavaScript plugin built in.Make sure you set one of your Tabs to `active` via your data.

<Example component="Tabs" variant="Basic" />


## API

### Tabs

<Api component={frontmatter.api.tabs} />
