---
title: Breadcrumb
description: Indicate the current page’s location within a navigational hierarchy that automatically adds separators via CSS.
test: '@layouts/Component.astro'
variant: component
api:
  breadcrumb:
    import: "import { Breadcrumb } from 'astro-bootstrap'"
    props:
    - name: aria-label
      type: string
      default: "'Breadcrumb'"
      description: Set the `aria-label` property value on the "breadcrumb" div.
    - name: id
      type: string
      default: 
      description: Set the `id` on the "breadcrumb" div.
    - name: class
      type: string
      default: 
      description: Set the `class` on the "breadcrumb" div.
    - name: itemClass
      type: string
      default: 
      description: Set the `class` on the "breadcrumb-item" div. Does not function if children are passed to this component.
    - name: children
      type: node | string
      default:
      description: Passing children disabled auto generation of breadcrumbs
  breadcrumbItem:
    import: "import { Breadcrumb } from 'astro-bootstrap'"
    props:
    - name: class
      type: string
      default:
      description: Set the `class` on the "breadcrumb-item" div.
    - name: active
      type: boolean
      default: 'false'
      description: Add's and `active` class.
    - name: children
      type: node | string
      default:
      description: Passing children disabled auto generation of breadcrumbs
---
import Example from '@components/Example.astro';
import Api from '@components/Api.astro';

## Examples

### Basic

<Example component="Breadcrumb" variant="Basic" />

### Set optional properties

<Example component="Breadcrumb" variant="Props" />

### Manual usage

<Example component="Breadcrumb" variant="Manual" />

## API

### Breadcrumb

<Api component={frontmatter.api.breadcrumb} />

### Breadcrumb.Item

<Api component={frontmatter.api.breadcrumbItem} />
