---
import ActiveLink from '../utils/ActiveLink.astro';
const { href, title, class: className, ...props} = Astro.props;
export interface Props {
  href: string;
  text?: string;
  class?: string;
}
console.log(title)
---
<ActiveLink href={href} class:list={['dropdown-item', className ]} class="dropdown-item" {...props}>
<slot>{title}</slot>
</ActiveLink>
