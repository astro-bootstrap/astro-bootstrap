---
import { Navbar } from 'astro-bootstrap';
// demonstration data
const siteTitle = 'Astro site'; // pull this in from a config file
const items = [
  // pull this in from a config file
  { title: 'Home', href: '#' },
  { title: 'About', href: '#' },
  {
    title: 'Dropdown',
    children: [
      { title: 'First child', href: '#' },
      { divider: true },
      { title: 'Second child', href: '#' },
    ],
  },
];
---

<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">{siteTitle}</a>
    <Navbar.Toggler ariaControls="navbarSupportedContent" />
    <Navbar.Collapse id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        {
          items.map((item) =>
            item.children ? (
              <Navbar.Dropdown {...item} />
            ) : (
              <Navbar.Item {...item} />
            ),
          )
        }
      </ul>
    </Navbar.Collapse>
  </div>
</nav>
